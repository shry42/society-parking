# Society-parking Firebase (society-parking-fb676)
# Copy this file to .env.local and fill values from Firebase Console:
# Project Settings → Your apps → Add app (Web) or use existing app config

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=society-parking-fb676.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=society-parking-fb676
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=society-parking-fb676.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Admin login: credentials are read from Firestore collection "admincred" (document "admin").
# The API route needs a service account key to read Firestore. Set one of:
# - FIREBASE_SERVICE_ACCOUNT_KEY_JSON = full JSON of service account key (one line)
# - GOOGLE_APPLICATION_CREDENTIALS = path to key file (e.g. scripts/society-parking-serviceAccountKey.json)
# Get key: Firebase Console → Project settings → Service accounts → Generate new private key.
FIREBASE_SERVICE_ACCOUNT_KEY_JSON=
